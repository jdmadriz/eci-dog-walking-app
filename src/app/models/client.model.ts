export interface Client {
    id: string; // UUID string
    firstName: string;
    lastName: string;
    email: string;
    phone: string;
    address: string;
    createdAt: string; // ISO date string
}